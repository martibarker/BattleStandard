/** Army Builder page — build and manage army lists */
export default function ArmyBuilder() {
  return (
    <div className="p-6">
      <h1 className="text-2xl" style={{ fontFamily: 'var(--font-heading)' }}>Army Builder</h1>
      <p style={{ color: 'var(--color-text-secondary)' }} className="mt-2 text-base">
        Build and validate your army lists here.
      </p>
    </div>
  );
}
