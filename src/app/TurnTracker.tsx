/** Turn Tracker page — track game turns and phases */
export default function TurnTracker() {
  return (
    <div className="p-6">
      <h1 className="text-2xl" style={{ fontFamily: 'var(--font-heading)' }}>Turn Tracker</h1>
      <p style={{ color: 'var(--color-text-secondary)' }} className="mt-2 text-base">
        Track game turns and phases.
      </p>
    </div>
  );
}
